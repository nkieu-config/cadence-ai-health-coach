# Board — Cadence

> [!NOTE]
> **บันทึกย้อนหลัง** — บอร์ดนี้หยุดไว้ที่ 25 ก.ค. 2569 ซึ่งเป็นสถานะก่อน pitch (30 ก.ค.)
> เก็บไว้ตามที่มันเป็นจริงตอนนั้น ไม่ได้ไล่แก้ให้ทันสมัย · สถานะปัจจุบันของโค้ดอ่านจาก [README](/README.md)

## Pitch 30 ก.ค. · Code freeze 29 ก.ค.

## สถานะ: เสร็จ 65 / 69 งาน (ยกเลิก 1 · เหลือ 3)

✅ **Deliverable ครบ 14/14 แล้ว** — งานเขียนโค้ดที่โจทย์บังคับจบหมด · ที่เหลือคือ QA + การนำเสนอ

**ของที่ต้องรู้ตอนนี้:**

- โมเดล production = `gemini-3.1-flash-lite` โควตา **500 ครั้ง/วันทั้งแอปรวมกัน** (INFRA-23) — ถ้า `.env.local` ของใครยังมี `AI_MODEL` ให้ลบทิ้ง
- บัญชี demo **`palm@example.com** — มีข้อมูล 24 วัน + reflection 4 สัปดาห์ + pattern cache ไว้แล้ว เปิดมาเห็นเลย
- **บัญชีเทส `qa-bot@example.com`** — ข้อมูล 24 วันเหมือนปาล์ม สร้างไว้ให้เทสได้โดยไม่ต้องแตะปาล์ม · เละแล้วรัน `DEMO_EMAIL=qa-bot@example.com npm run seed` คืนสภาพ
- migration ล่าสุดบน production = **0005** (ตัวนับโควตาแก้เองไม่ได้แล้ว)
- CI บังคับ 2 ด่านทุก PR: `verify` (format/lint/tsc/test/build) + `e2e (เปิดแอปจริง)`

## งานที่เหลือ

| Issue | งาน | ใคร | หมายเหตุ |
| --- | --- | --- | --- |
| qa/02 | QA เต็มรอบ | ทุกคน | จับเวลาครบแล้ว (24 ครั้ง) · **FR ระดับ M เดินครบบน production แล้ว 25 ก.ค.** · เหลือ 3 อย่างที่เครื่องมือทำแทนไม่ได้ → **Google OAuth · เครื่องจริง 3 แบบ (iPhone Safari / Android / โปรเจกเตอร์)** · ดู [`qa-results.md`](qa-pitch/qa-results.md) |
| qa/03 | Pitch deck + ซ้อม | A + ทุกคน | **`deck.pdf` 14 หน้าเสร็จแล้ว** (10 + สำรอง 4 · ชื่อทีมขึ้นปกแล้ว) สร้างจาก `deck/deck.html` ด้วย `npm run deck` — ไม่ได้ทำใน Canva แก้แล้วสั่งสร้างใหม่ได้ · เหลือ **แบ่งคนพูด** + ยืนยันเวลากับอาจารย์ + เลือกโทนเด็คตอนเทสโปรเจกเตอร์ + ซ้อม 2 รอบ 29 ก.ค. · เริ่มที่ docs/pitch/README |
| f2/05 | Streak | 🟦 | ตัดได้ (Priority C) |
| ~~f4/06~~ | ~~markdown ในฟองแชท~~ | A | ✅ ปิดแล้ว 25 ก.ค. |
| ~~infra/20~~ | ~~อุดช่องโหว่โควตารีเซ็ต~~ | A | ✅ ปิดแล้ว 25 ก.ค. — migration 0005 ลง production · ยิงตรงด้วย JWT ผู้ใช้แล้วแก้/ลบตัวนับไม่ได้ |
| ~~infra/27~~ | ~~ปุ่มสลับ dark/light~~ | A | ✅ ปิดแล้ว (merge PR #88, 22 ก.ค.) |

3 สาย UI (🟦 กราฟ · 🟩 โค้ช · 🟨 สิทธิ์+เป้าหมาย) **หมดคิวบังคับแล้วทั้งหมด**

## บั๊กค้างจาก QA-02 (ไม่ได้เปิดเป็น issue — อยู่ในตารางข้อ 5 ของ [qa-results.md](qa-pitch/qa-results.md))

| # | อาการ | severity | สถานะ |
| --- | --- | --- | --- |
| 1 | รายงานสัปดาห์เอาเลขที่ถูกไปแปะป้ายผิด เช่น "อัตราการข้ามมื้อ**เช้า** 80%" ทั้งที่สถิตินั้นคือข้ามมื้อไหนก็ได้ · "**เวลาเข้านอน**เฉลี่ย 6.6 ชั่วโมง" | กลาง | ✅ แก้แล้ว 25 ก.ค. — ⚠️ **แต่ของที่ cache ไว้ในบัญชีปาล์มยังเป็นข้อความเก่า ต้องรัน `npm run refresh:demo-week` ถึงจะหาย** |
| 2 | เรียก disruptor 3 ชื่อ: "วันพิเศษ" · "ปัจจัยรบกวน" (ไฟล์เดียวกัน) · "สิ่งรบกวนตาราง" | ต่ำ | ✅ แก้แล้ว 25 ก.ค. — เหลือ **"ปัจจัยรบกวน"** คำเดียวทั้ง UI/prompt/คำตอบ AI · ปักคำไว้ใน CONTEXT.md แล้วกันเพี้ยนซ้ำ |
| 3 | ลบข้อมูลทั้งหมดแล้วเด้งไป onboarding เงียบ ๆ | กลาง | ✅ แก้แล้ว 25 ก.ค. |
| 4 | โค้ชโยงอาการที่ผู้ใช้เอ่ย (เช่น ปวดหัว) เข้ากับ pattern ที่บันทึกไว้ ทั้งที่ระบบไม่เคยเก็บข้อมูลอาการ — ยิง 3 ครั้งเจอ 2 | ต่ำ–กลาง | ✅ แก้แล้ว 26 ก.ค. — แก้ที่บล็อกบริบท ไม่ใช่ system prompt หลักฐาน 20/20 จึงไม่กระทบ · ยืนยันด้วย checklist 20 นัดในโหมดมีบริบท (ชุดใหม่ใน `ai-safety-test/`) |
| 5 | หน้าโค้ชสูงเกินจอบนมือถือ — **ที่ 320px เมนูล่างทับช่องพิมพ์** และ **แผงตั้งเป้าหมายถูกการ์ดตัดจนกดปุ่ม "ถัดไป" ไม่ได้ที่ 320/375/390** · เดสก์ท็อป/โปรเจกเตอร์ไม่กระทบ | **สูง** | ✅ แก้แล้ว 26 ก.ค. — แก้ค่าหักความสูง + หัก `safe-area-inset-bottom` + ให้แผงล่างเลื่อนเองได้ · มีด่าน `e2e/coach.spec.ts` คุมแล้ว (พิสูจน์ว่าล้ม 5/7 กับโค้ดเก่า) |

## กติกาทีม

1. **เปิด issue ก่อนเขียนโค้ด** · 1 issue = 1 branch = 1 PR → `feat/<เลข issue>-<คำสั้น>`
2. **ข้อความ commit และชื่อ PR เขียนเป็นภาษาอังกฤษ** — เว้นคำที่เป็นข้อความบนจอหรือศัพท์ที่กำลังปักคำ (เช่น `"ปัจจัยรบกวน"`) ให้คงไทยในเครื่องหมายคำพูด · เอกสารในเรโปยังเป็นไทยตามเดิม
3. **sync main 3 จังหวะ:** ก่อนเริ่มงาน · **ก่อน push/เปิด PR** ⭐ · เมื่อมีคนประกาศ merge

   ```bash
   git checkout main && git pull
   git checkout <branch ตัวเอง> && git merge main
   ```

4. **อยู่ในโซนไฟล์ของสายตัวเอง** — ต้องแตะนอกโซน **แจ้งกลุ่มก่อน** · **ห้ามลง npm package เอง** (lock file ชนกัน) → แจ้ง A
5. **ห้ามแตะ Supabase / Gemini ตรง ๆ** — เรียกผ่าน `src/lib/` เท่านั้น · type กลางอยู่ที่ `@/lib/domain` · วันที่ใช้ `today()`/`daysAgo()` จาก `lib/checkins/date.ts` ห้าม `new Date()`
6. **เช็ค `git config user.email`** ให้ตรงกับอีเมลที่ Verified บน GitHub ก่อน commit แรก — ไม่งั้นผลงานไม่ถูกนับ
7. **อย่าเทสด้วยบัญชีปาล์ม** — สมัครบัญชีทิ้งแทน · ปาล์มคือบัญชีที่ใช้ demo วัน pitch (ข้อความเทสจะโผล่บนจอตอนนำเสนอ)

กฎอื่นอยู่ในไฟล์ที่เป็นเจ้าของเรื่อง: ขั้นตอนก่อนเปิด PR → [CONTRIBUTING.md](/CONTRIBUTING.md) · กฎ UI → [DESIGN.md](/DESIGN.md) ส่วนแรก · กติกาการทำงาน → [docs/agents.md](/docs/agents.md)

## ก่อนวัน pitch

รัน `npm run refresh:demo-week` (อุ่น goal + insight/reflection ของปาล์มให้ตรงวัน — ของพวกนี้ผูกกับวันที่ จึงเก่าเองทุกวัน)
