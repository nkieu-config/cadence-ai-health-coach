# F2-02: กราฟ 3 pillars + energy

Status: done
Owner: 🟦 กราฟ
Sprint: 1
Priority: M
Refs: FR-2.1, docs/05
Blocked by: 01

## งาน

- [x] นอน: กราฟแท่งชั่วโมงนอนรายวัน + เส้นอ้างอิง 6 ชม. (จุดสังเกต ไม่ใช่เกณฑ์ตัดสิน)
- [x] กิน: มื้อที่กิน/ข้ามรายวัน + จำนวนเครื่องดื่มหวาน
- [x] เคลื่อนไหว: นาทีเคลื่อนไหวรายวัน แยกสีตามชนิด
- [x] Energy: แถบ low/medium/high รายวัน วางให้เทียบสายตากับกราฟอื่นได้
- [x] วันที่ไม่มีบันทึกแสดงเป็นช่องว่าง ไม่ใช่ศูนย์ (กันกราฟโกหก)

## Acceptance criteria

- ดูกราฟนอน + energy คู่กันแล้ว "เห็น" ความสัมพันธ์ได้ด้วยตาจาก seed data
- ไม่มีการให้คะแนน/เกรดสุขภาพที่ไหนเลย

## Comments

---

15 ก.ค. (A) — kickoff · 🟦 ทำ component กราฟใน `src/components/dashboard/` + วางการ์ดใน dashboard page

**ระวัง:** สีใช้ `var(--chart-N)` เท่านั้น (นอน=1 · กิน=2 · เคลื่อนไหว=3) ห้าม `theme: { light, dark }` · วันไม่มีบันทึก = ช่องว่าง ไม่ใช่ 0 · ห้ามมีคะแนน/เกรดสุขภาพที่ไหนเลย · ชื่อไทยใช้จาก `@/lib/checkins/labels` ห้ามพิมพ์เอง

---

17 ก.ค. (A) — **ปิดงาน** · งานหลักโดย 🟦 ใน PR #46 · A แก้ต่อยอดใน commit ท้าย PR

**แก้สเปกของ issue นี้ (โดย A ผู้เขียนสเปกเอง):** "เคลื่อนไหว: แยกสีตามชนิด" — ข้อมูลจริงรองรับไม่ได้ (check-in เก็บนาทีรวม ไม่ได้เก็บนาทีต่อชนิด) → เป็นแท่งนาทีรวมสีเดียว `--chart-3` แทน

**ข้อตกลงที่ยังมีผล:** ห้ามถอด `CurrentGoalCard` + `ReflectionCard` ออกจากหน้า — ลิงก์ไป `/reflection` มีที่เดียวในแอปคือการ์ดใบนี้ และไม่มีเทสต์ตัวไหนจับ · Energy ใช้สีเดียว `--chart-4` ห้ามใช้แดง=ต่ำ/เขียว=สูง (CVD) · goal ทดสอบค้างของปาล์ม 2 อัน ต้องล้างก่อน pitch → QA-03

---

17 ก.ค. (A) — ⚠️ **แก้คำแถลงข้างบนที่ผิด** · ดู **F2-06**

"เปลี่ยนเป็นแท่งนาทีรวมสีเดียว" **ไม่จริง ณ ตอนที่เขียน** — ไม่ได้แตะ JSX ทำให้แท็บเคลื่อนไหวว่างเปล่าตั้งแต่ commit นั้น · 🟦 เป็นคนรายงาน · ตามแก้ใน F2-06
