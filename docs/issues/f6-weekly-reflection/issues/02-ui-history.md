# F6-02: Reflection UI + ดูย้อนหลัง

Status: done
Owner: A (ย้ายมาจาก 🟨 เมื่อ 16 ก.ค.)
Sprint: 3
Priority: M (ย้อนหลัง = S)
Refs: FR-6.1, FR-6.2
Blocked by: 01, F6-03

## งาน

- [x] หน้า reflection สัปดาห์ล่าสุด อ่านง่าย แบ่ง section ตามโครง
- [x] เลือกดูสัปดาห์ก่อน ๆ ได้ (จาก `ai_outputs` cache)
- [x] ปุ่มต่อยอด: "ตั้งเป้าสัปดาห์หน้า" → เข้า guided flow F4-03 (เกณฑ์ Reflection and Improvement)

## Acceptance criteria

- Demo account เห็น reflection ≥ 2 สัปดาห์
- กดจาก reflection ไปตั้ง goal ได้จบเส้น

## Comments

---

15 ก.ค. (A): kickoff ส่งให้ 🟨 · **ไม่ต้องรอ F6-01** — reflection จริงมาเสียบทีหลังหน้าตาเดิม · **ห้ามแตะ dashboard และ `reflection-card.tsx`**

ปุ่ม "ตั้งเป้าสัปดาห์หน้า" ลิงก์ไป `/goals` ไปก่อน แล้วสลับเป็น guided flow เมื่อ F4-03 เสร็จ · AC "เห็น ≥ 2 สัปดาห์" ต้องรอข้อมูลจาก F6-01

---

15 ก.ค. (A): F6-01 เสร็จแล้ว — `Reflection` เพิ่มฟิลด์ `strengths` **ไม่บังคับใช้** ของเดิมไม่ต้องแก้อะไร

---

16 ก.ค. (A): ✅ **ข้อมูล demo พร้อมแล้ว (INFRA-16)** — ปาล์มมี reflection 4 สัปดาห์ cache บน production เปิดมาเห็นเลย **ไม่ต้องกดปุ่ม generate** (กันโควตา)

---

16 ก.ค. (A): 🔄 **ย้ายงานนี้มาให้ A ทำเอง — 🟨 ไม่ต้องทำแล้ว** · คิวของ 🟨 เหลือ F7-02 → F5-02 · ไม่ต้องแตะ `app/(app)/reflection/` และ `components/reflection/` แล้ว

เหตุผล: เกณฑ์ "เห็นพัฒนาการ" ต้องมีการเทียบสัปดาห์ต่อสัปดาห์ใน engine ก่อน (F6-03) ซึ่งเป็นงานฝั่ง A

---

16 ก.ค. (A): เสร็จ · branch `feat/f6-02-reflection-page` · ปิด AC ทั้งสองข้อ · รวมการ์ด "เทียบกับสัปดาห์ก่อน" จาก F6-03 (สัปดาห์แรกที่ไม่มีอะไรเทียบ → ซ่อนเอง)

ข้อความ AI ถูก cache ไว้ ส่วนตัวเลขเทียบคำนวณสด → ขัดกันได้ถ้ามีบันทึกเพิ่มระหว่างวัน · ไม่กลบ แต่ขึ้นการ์ด "สรุปนี้ยังไม่รวมบันทึกล่าสุดของคุณ" + ปุ่มสร้างใหม่ เฉพาะสัปดาห์ล่าสุด

**ฝากไว้ใน QA-03:** ก่อน pitch ต้องกด "สร้างสรุปใหม่" ของสัปดาห์ล่าสุด 1 ครั้ง

---

17 ก.ค. (A): ปุ่ม "สร้างสรุปใหม่" พิสูจน์แล้วด้วย Gemini จริง — กดแล้วการ์ดเตือนหายไปเอง ปิดครบทั้งเส้น

**ยังต้องทำซ้ำก่อน pitch (QA-03)** — ถ้ามีใครเช็คอินให้ปาล์มเพิ่ม สรุปจะเก่าอีกครั้ง
