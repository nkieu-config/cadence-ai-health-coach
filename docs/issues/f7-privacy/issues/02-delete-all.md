# F7-02: ลบข้อมูลทั้งหมด / ลบบัญชี

Status: done
Owner: 🟨 สิทธิ์+เป้าหมาย
Sprint: 1
Priority: M — ห้ามตัด (เกณฑ์ Privacy)
Refs: FR-7.2, docs/05 (on delete cascade)
Blocked by: 01

## งาน

- [x] ปุ่ม "ลบข้อมูลทั้งหมด" (เหลือบัญชี) และ "ลบบัญชีถาวร" — confirm 2 ชั้นพิมพ์ยืนยัน
- [x] ลบบัญชีผ่าน service role ฝั่ง server → cascade ลบทุกตาราง
- [x] ทดสอบว่าไม่เหลือข้อมูลตกค้างตารางไหนเลย

## Acceptance criteria

- ลบบัญชีแล้ว query ทุกตารางด้วย user_id เดิม = 0 แถว (บันทึกหลักฐานใน Comments)

## Comments

---

15 ก.ค. (A) — kickoff · 🟨 ทำแค่ UI + confirm ในหน้า privacy · ส่วนที่แตะ service role (`deleteAllData()` / `deleteAccount()` ใน `@/lib/account/actions`) A เขียนให้หมดแล้ว

**ระวัง:** ปุ่ม "ลบบัญชี" บนเครื่อง 🟨 จะขึ้นว่าไม่มี service role key — **ปกติ ไม่ใช่โค้ดผิด** (key อยู่กับ A คนเดียว) · ทดสอบกับ**บัญชีสมัครใหม่ทิ้ง ๆ เท่านั้น ห้ามใช้บัญชีตัวเอง เพื่อน หรือปาล์ม** · AC (0 แถวทุกตาราง) A รันปิดให้บน production

---

16 ก.ค. (A) — ⚠️ **หน้า privacy ถูก polish แล้ว (INFRA-18)** โครงเปลี่ยนนิดหน่อย ทำ F7-02 ต่อได้เลย ไม่ชนกัน
เอา section ลบไปต่อเป็นการ์ดตัวสุดท้ายของหน้า (ใต้การ์ด "สิทธิ์การจัดการข้อมูล")

---

17 ก.ค. (A) — **ปิดงาน** · UI ของ 🟨 อยู่ใน PR #43 · A รัน AC ปิดให้ตามที่นัดไว้ตอน kickoff

**AC ผ่านทั้งสองเส้น** (ทดสอบผ่าน UI จริงด้วยบัญชีสมัครทิ้ง ไม่แตะปาล์ม): ลบข้อมูลทั้งหมด → ทุกตาราง 0 แถว บัญชียังอยู่ ✅ เด้ง `/onboarding` · ลบบัญชีถาวร → query ไม่เจอ user ✅ เด้ง `/login?deleted=1`
A แก้เพิ่มใน PR #43: คืนหน้า privacy เป็น server component (แตก `delete-zone.tsx`) · แก้คำพัง `พฤทีธรรม` → `พฤติกรรม` · ผูก label/input · ใช้ `useTransition` · ไม่ `router.push` ทับตอนลบบัญชี
