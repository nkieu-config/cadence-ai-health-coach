# ของนำเสนอวัน pitch (30 ก.ค. 2026) — QA-03

โฟลเดอร์นี้คือบ้านของทุกอย่างที่ใช้บนเวที · เกณฑ์และเนื้อเรื่องอ้างจาก [docs/10](../10-deliverables-checklist.md)

| ที่ | อะไร | สถานะ |
|---|---|---|
| ⭐ [slide-content.md](slide-content.md) | **ต้นฉบับเนื้อหา 8 หน้า + สำรอง 3** — ข้อความบนสไลด์ + สคริปต์ปาก + ตารางเกณฑ์ 9 ข้อ + Q&A ซ้อมตอบ | ✅ |
| [deck-outline.md](deck-outline.md) | โครงร่างแรก 13 หน้า — **เก็บเป็นคลังเหตุผล/speaker note เท่านั้น เลขสไลด์ไม่ตรงกับของจริง** | 📚 อ้างอิง |
| [demo-script.md](demo-script.md) | บทเดิน demo รายคลิก + ใครพูดท่อนไหน + **ขั้นตอนเตรียมวันจริงทั้งหมด** | ✅ ร่างแล้ว (รอเติมชื่อคนพูด) |
| **`deck.pdf`** | **สไลด์จริง 11 หน้า 16:9 (โทนมืด)** — สร้างจาก [deck/deck.html](deck/deck.html) ด้วย `npm run deck` | ✅ |
| `deck-light.pdf` | ชุดเดียวกันโทนสว่าง — เผื่อโปรเจกเตอร์สู้แสงไม่ไหวจนพื้นมืดกลายเป็นสีโคลน | ✅ |
| [`screenshots/`](screenshots/) | ภาพสำรองทุกจอสำคัญ — plan B เมื่อเน็ต/AI ล่มกลางเวที | ✅ สร้างด้วย `npm run shots` |

## แก้สไลด์ยังไง

**ไม่ได้ทำใน Canva** — เด็คเป็น HTML ในเรโป แก้แล้วสั่งสร้างใหม่ได้ ไม่ต้องจัดหน้าเองทุกครั้ง

```bash
# แก้ docs/pitch/deck/deck.html แล้ว
npm run deck        # → deck.pdf + deck-light.pdf
```

ตัวสร้างตรวจให้ทุกครั้ง: **เนื้อหาล้นหน้า** (ทั้งระดับสไลด์และกล่องข้างใน) · ภาพเสีย · ฟอนต์ไทยไม่ติด · ตัวไทยต่ำกว่า 18px — เจอแล้วขึ้น ✗ และคืน exit code ไม่เป็นศูนย์

ฟอนต์ IBM Plex Sans Thai ตัวเดียวกับแอปฝังอยู่ใน [deck/fonts/](deck/fonts/) แล้ว · **เปิดและสร้างใหม่ได้โดยไม่ต้องมีเน็ต**

## งานที่เหลือ

รายการเต็มอยู่ท้าย [slide-content.md](slide-content.md) หัวข้อ "เช็กก่อนถือว่าเด็คเสร็จ" — ตัวใหญ่ ๆ คือ:

1. **เติมชื่อ 4 คน** — แก้ `⟨ ชื่อสมาชิก 4 คน ⟩` ในหน้าปกของ [deck/deck.html](deck/deck.html) แล้วรัน `npm run deck` · และแบ่งคนพูดใน demo-script (ยังว่าง 4 จาก 5 องก์)
2. **ยืนยันเวลาจริงกับอาจารย์** — ตอนนี้จัดไว้ 8:00 พอดีไม่มี buffer
3. **เลือกโทนเด็ค** — ลองฉาย `deck.pdf` (มืด) กับ `deck-light.pdf` (สว่าง) บนโปรเจกเตอร์จริง แล้วเก็บตัวที่ชนะ
4. รัน `npm run shots` ซ้ำหลัง freeze ให้ภาพตรงกับของจริงวันนำเสนอ **แล้วรัน `npm run deck` ตาม** (เด็คใช้ภาพจากโฟลเดอร์เดียวกัน)
5. ซ้อมเต็ม 2 รอบวันที่ 29 ก.ค. (checklist ซ้อม + กฎเหล็กวันจริงอยู่ท้าย demo-script)

> ~~ทำสไลด์สำรอง~~ — เสร็จแล้ว 3 หน้า (A/B/C) อยู่ท้ายเด็ค

> ขั้นตอนเช้าวัน pitch (รัน `npm run refresh:demo-week`, เคลียร์ stray goal, ข้อห้ามเรื่องโควตา) รวมอยู่ที่เดียวใน [demo-script.md](demo-script.md) — ไม่เขียนซ้ำที่นี่

## `npm run shots`

ถ่ายทุกหน้า × มือถือ/เดสก์ท็อป × light/dark (28 รูป) พร้อมเช็คว่าไม่มี console error · ไม่มีการเลื่อนแนวนอน · h1 หน้าละอัน

ใช้ 3 อย่างด้วยสคริปต์เดียว: รีวิวดีไซน์ตอนแก้ · แนบหลักฐาน before/after ใน PR · **ภาพสำรองวัน pitch**

เก็บเข้าเรโปเฉพาะชุด `light-desktop` (ชุดที่ฉายขึ้นจอ และเป็นตัวอย่างให้กรรมการดูโดยไม่ต้องล็อกอิน) · ชุดมือถือกับ dark เป็น gitignore เพราะสร้างใหม่ได้ตลอด
