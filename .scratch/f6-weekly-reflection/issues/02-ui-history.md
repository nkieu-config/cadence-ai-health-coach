# F6-02: Reflection UI + ดูย้อนหลัง

Status: done
Owner: A (ย้ายมาจาก 🟨 เมื่อ 16 ก.ค.)
Sprint: 3
Priority: M (ย้อนหลัง = S)
Refs: FR-6.1, FR-6.2
Blocked by: 01, F6-03

## งาน

- [x] หน้า reflection สัปดาห์ล่าสุด อ่านง่าย แบ่ง section ตามโครง
- [x] เลือกดูสัปดาห์ก่อน ๆ ได้ (จาก `ai_outputs` cache)
- [x] ปุ่มต่อยอด: "ตั้งเป้าสัปดาห์หน้า" → เข้า guided flow F4-03 (เกณฑ์ Reflection and Improvement)

## Acceptance criteria

- Demo account เห็น reflection ≥ 2 สัปดาห์
- กดจาก reflection ไปตั้ง goal ได้จบเส้น

## Comments

---

15 ก.ค. (A) — kickoff · **ไม่ต้องรอ F6-01** — โครงพร้อม reflection จริงมาเสียบทีหลังหน้าตาเดิม

**ไฟล์**

- แก้ `src/app/(app)/reflection/page.tsx` — แทน placeholder · สร้าง `loading.tsx` คู่กัน
- component เพิ่มลง `src/components/reflection/` (การ์ดบน dashboard มีแล้ว: `reflection-card.tsx` — ห้ามแตะ dashboard)

**เรียกใช้** — ทั้งหมดจาก `@/lib/ai-outputs/*`

- `getLatestReflection()` → `Reflection | null` · `null` = โชว์ปุ่ม "สร้างสรุปสัปดาห์" → `generateReflection()` (~10 วิ **ผูกปุ่มเท่านั้น**)
- `getReflections()` → ย้อนหลังทั้งหมด ใหม่→เก่า — ไว้ทำตัวเลือกดูสัปดาห์ก่อน (FR-6.2)
- โครง `Reflection`: `{ daysRecorded, totalDays, pillars: [{ pillar, summary }], nextWeek, periodStart, periodEnd }`

**ทำ:** หน้าอ่านสรุปล่าสุด แบ่ง section ตาม `pillars` + `nextWeek` · เลือกดูสัปดาห์เก่า · ปุ่ม "ตั้งเป้าสัปดาห์หน้า" → **ลิงก์ไป `/goals` ไปก่อน** (สลับเป็น guided flow เมื่อ F4-03 เสร็จ — จะมาคอมเม้นบอก)

**ระวัง:** AC "เห็น ≥ 2 สัปดาห์" ต้องรอ F6-01 ของ A สร้างข้อมูลจริงให้ปาล์ม — UI คุณจบก่อนได้เลย

---

15 ก.ค. (A) — F6-01 เสร็จแล้ว · **มีฟิลด์ใหม่เพิ่มเข้ามา ไม่บังคับใช้**

`Reflection` type เพิ่ม `strengths: string` (จุดแข็งที่ควรรักษาของสัปดาห์นั้น — ข้อความสั้น 1 ย่อหน้า เหมือน `nextWeek`) ของเดิมทั้งหมด (`daysRecorded` `totalDays` `pillars` `nextWeek`) **ยังหน้าตาเดิมทุกอย่าง ไม่ต้องแก้โค้ดที่มีอยู่**

ถ้าอยากโชว์เป็น section แยกในหน้า reflection ก็ใช้ `reflection.strengths` ได้เลย (ไม่บังคับ ถ้าไม่มีเวลาไม่ใส่ก็ได้ ไม่ทำให้อะไรพัง)

---

16 ก.ค. (A) — ✅ **ข้อมูล demo พร้อมแล้ว (INFRA-16)** ไม่ต้องรออะไรแล้ว

ปาล์มมี reflection **4 สัปดาห์** cache บน production — `getReflections()` คืน 4 อัน, `getLatestReflection()` มีของจริง · AC "เห็น ≥ 2 สัปดาห์" ผ่านทันทีที่ต่อ UI · เปิดมาเห็นข้อมูลจริงเลย **ไม่ต้องกดปุ่ม generate** (กัน quota)

---

16 ก.ค. (A) — 🔄 **ย้ายงานนี้มาให้ A ทำเอง — 🟨 ไม่ต้องทำแล้ว**

**ไม้ 🟨 อ่านตรงนี้:** คิวของคุณเหลือ **F7-02 → F5-02** เท่านั้น · ไม่ต้องแตะ `app/(app)/reflection/` และ `components/reflection/` แล้ว จะได้ไม่ชนกับ A

**ทำไมถึงย้าย** — ไม่ใช่เรื่องความเร็วของใคร แต่เป็นเรื่องที่งานนี้กลายเป็นงานเครื่องยนต์ไปแล้วครึ่งหนึ่ง:

เกณฑ์ **Reflection and Improvement** ในโจทย์เขียนว่า *"ระบบช่วยให้ผู้ใช้เห็น**พัฒนาการ**และปรับเป้าหมายต่อได้"* — ตรวจโค้ดแล้วทั้ง `src/lib` **ไม่มีการเทียบสัปดาห์ต่อสัปดาห์อยู่เลยสักจุด** `Reflection` เป็นภาพนิ่งของสัปดาห์เดียว ไม่รู้จักสัปดาห์ก่อนหน้า · ต่อให้ทำ UI ตามสเปกเดิมครบทุกข้อ ก็จะได้แค่ "รายการ reflection หลายสัปดาห์" ซึ่ง**ยังไม่ใช่ "เห็นพัฒนาการ"** → ต้องเพิ่มของใน engine ก่อน (F6-03) แล้วค่อยวาด UI ให้เห็น ซึ่งงาน engine เป็นของ A อยู่แล้วตามการแบ่งงาน

**ขอบเขตที่ A จะทำ:** ตามสเปกเดิมทุกข้อ + เพิ่มส่วนเทียบกับสัปดาห์ก่อนหน้าจาก F6-03

---

16 ก.ค. (A) — เสร็จ · branch `feat/f6-02-reflection-page`

**ทำอะไร:** `src/app/(app)/reflection/page.tsx` แทน placeholder · เลือกสัปดาห์ผ่าน `?week=<periodStart>` (server-rendered ล้วน ไม่มี state ฝั่ง client · แชร์ลิงก์/กด back ได้ตามกฎ deep linking) · component ใหม่ใน `src/components/reflection/`: `week-picker` · `week-comparison-card` · `generate-reflection-button` · **ไม่แตะ dashboard และ `reflection-card.tsx` ตามที่ kickoff สั่ง**

**ปิด AC ทั้งสองข้อ:**

| AC | ผล |
|---|---|
| Demo account เห็น reflection ≥ 2 สัปดาห์ | ✅ ปาล์มเห็นครบ **4 สัปดาห์** — ปุ่มเลือกสัปดาห์ 4 ปุ่ม กดเปิดสัปดาห์เก่าได้จริง (ทดสอบ `?week=2026-06-26`) |
| กดจาก reflection ไปตั้ง goal ได้จบเส้น | ✅ ปุ่ม "ตั้งเป้าสัปดาห์หน้า" ท้ายการ์ด "ข้อเสนอสำหรับสัปดาห์หน้า" → `/goals` (สลับเป็น guided flow เมื่อ F4-03 เสร็จ — คอมเมนต์ไว้ใน F4-03 แล้ว) |

**เพิ่มจาก F6-03:** การ์ด "เทียบกับสัปดาห์ก่อน" — ตัวปิดเกณฑ์ Reflection and Improvement · สัปดาห์เก่าสุดที่ไม่มีอะไรให้เทียบ → ซ่อนการ์ดอัตโนมัติ

**บั๊กที่เจอตอนดูภาพหน้าจอจริง (ไม่เจอจาก test):** ข้อความสรุปกับตัวเลขเทียบ**ขัดกันเองบนจอเดียวกัน** — ข้อความบอก "บันทึก 6 จาก 7 วัน · นอนเฉลี่ย 6.8" แต่ตัวเลขเทียบบอก "7 วัน · 6.5" · สาเหตุ: ข้อความ AI ถูก cache ไว้ตอน 06:26 (ตอนนั้นปาล์มมี 6 วัน) แต่การเทียบคำนวณสด (ตอนนี้มี 7 วัน เพราะมีคนเช็คอินให้ปาล์มเพิ่มระหว่างวัน)

**แก้ยังไง:** ไม่กลบ แต่บอกผู้ใช้ตรง ๆ — การ์ด "สรุปนี้ยังไม่รวมบันทึกล่าสุดของคุณ" + ปุ่มสร้างใหม่ ใช้ `isFresh(createdAt, latestCheckinAt)` ที่มีอยู่แล้วในระบบ · โผล่เฉพาะสัปดาห์ล่าสุดเท่านั้น (สัปดาห์เก่าข้อมูลนิ่งแล้ว ยืนยันด้วยภาพหน้าจอ: สัปดาห์ 26 มิ.ย. ไม่ขึ้นการ์ดนี้ และตัวเลขตรงกันหมด)

**⚠️ ฝากไว้ใน QA-03:** ก่อน pitch ต้องกด "สร้างสรุปใหม่" ของสัปดาห์ล่าสุด 1 ครั้ง (1 Gemini call) ให้ข้อความตรงกับข้อมูลล่าสุด จะได้ไม่ต้องอธิบายการ์ดเตือนบนเวที

**พิสูจน์:** e2e 30/30 · format/lint/tsc/test 131 · build ผ่าน · screenshot มือถือ + เดสก์ท็อป + สัปดาห์เก่า · **ไม่ยิง Gemini เลยสักครั้ง** (ปาล์มมี cache ครบอยู่แล้ว)
