# F4-05: ลบประวัติแชท

Status: done
Owner: 🟩 โค้ช
Sprint: 2
Priority: S
Refs: FR-4.5, FR-7.x
Blocked by: 01

## งาน

- [x] ปุ่มลบประวัติแชททั้งหมดในหน้าแชท (มี confirm)
- [x] ลบจริงจาก `chat_messages` ไม่ใช่ซ่อน

## Acceptance criteria

- ลบแล้ว query DB ไม่เหลือแถวของ user นั้น

## Comments

---

15 ก.ค. (A) — kickoff (ทำต่อจาก F4-01)

**ไฟล์:** ปุ่ม + confirm ใน `src/components/coach/` แล้ววางในหน้าแชท

**เรียกใช้:** `clearChatHistory()` — `@/lib/chat/actions` · "ลบจริงไม่ใช่ซ่อน" action จัดการแล้ว (ลบแถวจาก DB ผ่าน RLS) · คืน `{ error }` เมื่อพลาด — แสดงตรง ๆ

**ปิด AC:** ลบแล้ว `getChatHistory()` ต้องว่าง + รีเฟรชหน้าก็ยังว่าง

---

16 ก.ค. (A) — **ปิดงาน · โค้ดของ 🟩 เข้า main แล้วผ่าน PR #33**

งานนี้ถูกทำรวมมาใน PR ของ F4-01 (คนละ issue แต่ PR เดียว) เลยไม่มีใครปิด issue นี้ — โค้ดขึ้น production ไปแล้วแต่บอร์ดยังอ่านว่ายังไม่เริ่ม · A รัน AC ปิดให้ ไม่ต้องดึง 🟩 กลับมาทำเอกสาร ไปต่อ F4-03 ได้เลย

**โค้ดที่เข้า main:** ปุ่ม "ล้างประวัติ" ใน `src/components/coach/chat-client.tsx` · double-confirm (กดครั้งแรกปุ่มเปลี่ยนเป็นสีแดง "ยืนยันล้างแชท" · ยกเลิกเองใน 5 วิถ้าไม่กดต่อ) · เรียก `clearChatHistory()` ซึ่งลบแถวจริงผ่าน RLS

**ปิด AC — ทดสอบผ่าน UI จริงด้วยบัญชีสมัครทิ้ง (`qa-f4-05-throwaway@…` ลบทิ้งแล้ว ไม่ยุ่งกับปาล์ม · ไม่กินโควตา Gemini เลย):**

| จังหวะ | ผล |
|---|---|
| ก่อนกด | DB 3 แถว · บนจอ 3 ฟองข้อความ |
| กดครั้งแรก | ปุ่มเปลี่ยนเป็น "ยืนยันล้างแชท" ✅ (ไม่ลบทันที) |
| กดยืนยัน | บนจอเหลือ 0 ฟอง |
| รีเฟรชหน้า | ยังเหลือ 0 ฟอง (ไม่ใช่แค่ซ่อนใน state) |
| **query DB** | **`chat_messages` = 0 แถว** ✅ **AC ผ่าน** |

**หมายเหตุที่ไม่ใช่บั๊ก:** ในภาพทดสอบลำดับฟองข้อความสลับกัน เพราะ A insert 3 แถวใน statement เดียว `created_at` เลยเท่ากันเป๊ะจนเรียงไม่ได้ — ของจริงข้อความห่างกันเป็นวินาที ไม่เจอปัญหานี้

**เรื่องที่ยังค้าง (ไม่ใช่ของ issue นี้):** ล้างประวัติ = โควตาแชท 5/วันรีเซ็ตด้วย เพราะ `countMessagesToday()` นับแถวใน `chat_messages` — เป็นดีไซน์ action ของ A เอง แยกเป็น INFRA-20
